// method chaining = calling one method after another in one continuos line of code

let userNme = "lukas";

let letter = userNme.charAt(0).toUpperCase().trim();
//letter = letter.toUpperCase();

console.log(letter);
